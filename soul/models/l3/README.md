# models/l3/ — L3 元认知模型（通用，脱敏）

准入条件：

- L3 = 关于自身认知过程的模型（学习策略、校准模式、纠偏机制），
  区别于 L2 世界行为模型（`../l2/`）。
- 只收经 declassification 链产出的通用 L3 模型衍生物；
  私有 L3（含个人信道地图/纠正史细节）永不进入本目录——
  它们属于 gdrive 私有资产备份面。
- 当前为空：首个达到发布标准的通用 L3 模型走 promotion 链进入。
