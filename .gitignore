__pycache__/
.claude/

# Control Plane generated runtime inventory (never canonical, never publish)
registry/inventory/

# Switchboard task milestone receipts (executor -> controller handoff)
output/

# Local runtimes, indexes, and dependency trees
*.py[cod]
**/__pycache__/
node_modules/
.grepai/
.taskflow/
Users/
.venv/
venv/

# Temporary inspection and generated deliverables
.tmp_*
.personal-ai-migration-backup-*
_make_summary*.py
build_weekly_summary.mjs
*.inspect.ndjson
*_inspect.ndjson
artifacts/
weekly_summary_*.xlsx
weekly_summary_*.png
weekly_summary_entries_*.json

# One-off local diagnostics (probe / schema dumps) — never publish
_probe*.py
_db_schema.py
_mem_src.py
_sqlite_src.py

# One-off local dsh probe / token audit scripts (repo root only) — never publish
/dsh-*.js
/dsh-*.json
scripts/summarize_all_sessions.js

# Local CLI-adapter design/survey docs (contain local model names & relay details) —
# keep on this machine, never publish to GitHub
docs/cli-adapter-*.md

# 本地运行态（flow 测试产物）
.workspace_local/

# DSH compaction-convergence overlay — device-local artifacts (rebuilt locally)
dsh/compaction-convergence/.dsh-convergence-backup/
dsh/compaction-convergence/test/.replay/
dsh/compaction-convergence/test/.last-deploy-marker.json

# Incident and stability closure staging artifacts
.incident-tmp/
.recovery-snapshots-*/
.stability-closure-*/

# Device-local deployment mirror + runtime lifecycle state (rebuilt locally, never canonical)
dsh-config/profiles/web/base-dsh-*/
dsh-config/profiles/web/.dsh-context-lifecycle/

# One-off verification scripts (repo root only) — never publish
/.verify-surface.mjs
