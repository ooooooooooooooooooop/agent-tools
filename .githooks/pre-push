#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"
PY="${PYTHON:-python3}"
"$PY" -c '' >/dev/null 2>&1 || PY=python

"$PY" scripts/governance/canonical_writer.py pre-push "$@"
