# claude.yaml — Claude Code harness contract（canonical · NATIVE INDEPENDENT）
# Personal AI 管理范围：READ_ONLY_DISCOVERY ONLY（禁止 AIC 写控制 / Hook 注入 / 代理劫持）
harness: claude
home: ".claude"
status: native_independent
version_observed: "2.1.258"

consumes: []
render_targets: []

overlay:
  - "All configuration in ~/.claude/ and ~/.claude.json is 100% user-owned and harness-native"

static_instructions:
  file: CLAUDE.md
  owner: user
  legality: "Official native instructions only; no AIC or Switchboard managed blocks"

runtime_context_hook:
  mechanism: "none (harness native independent)"
  constraint: "Personal AI does not inject runtime hooks or instructions"

health_check: "claude --version"
runtime_state: "projects/ sessions/ history.jsonl tasks/ plans/ — 永不入 canonical"
secret: "Official Anthropic auth / user-defined proxy — 永不入 canonical"

governance_hooks: {}
