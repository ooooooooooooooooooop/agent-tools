# codex.yaml — Codex harness contract（canonical · NATIVE INDEPENDENT）
# Personal AI 管理范围：READ_ONLY_DISCOVERY ONLY（禁止 AIC 写控制 / Hook 注入 / 强制 MCP 劫持）
harness: codex
home: ".codex"
status: native_independent
version_observed: "codex-cli 0.149.0"

consumes: []
render_targets: []

overlay:
  - "All configuration in ~/.codex/config.toml and ~/.codex/ is 100% user-owned and harness-native"

static_instructions:
  file: AGENTS.md
  owner: user
  legality: "Official native instructions only; no AIC or Switchboard managed blocks"

runtime_context_hook:
  mechanism: "none (harness native independent)"
  constraint: "Personal AI does not inject runtime hooks or instructions"

health_check: "codex --version"
runtime_state: "sessions/ *.sqlite logs/ models_cache.json thread-writer-locks/ — 永不入 canonical"
secret: "auth.json（ChatGPT OAuth）— 永不入 canonical"

governance_hooks: {}
