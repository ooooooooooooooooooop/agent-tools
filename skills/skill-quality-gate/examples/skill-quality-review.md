# Skill 质量审查示例

用户请求：

> 请用 `$skill-quality-gate` 审查 `natural-rewrite`，只报告问题，不修改文件。

最小输出应包括：

- 状态：`PASS`、`PARTIAL` 或 `BLOCKED`。
- 目标包、版本和实际读取的文件。
- 触发边界、流程、输出、验证和安全结论。
- 现有测试的命令与结果。
- 按优先级排列的最小改进项。
- 未验证行为和明确未修改范围。
